{{- if .Values.fastmoda.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "moda.fullname" . }}-fastmoda
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "moda.labels" . | nindent 4 }}
    app.kubernetes.io/component: fastmoda
    app.kubernetes.io/port: {{ .Values.fastmoda.service.port | quote }}
  {{- with .Values.fastmoda.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.fastmoda.service.type }}
  ports:
    - port: {{ .Values.fastmoda.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moda.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: fastmoda
{{- end }}

---
{{- if .Values.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "moda.fullname" . }}-server
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "moda.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
    app.kubernetes.io/port: {{ .Values.server.service.port | quote }}
  {{- with .Values.server.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: matlab
      protocol: TCP
      name: matlab
  selector:
    {{- include "moda.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
{{- end }}

---
{{- if .Values.mobile.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "moda.fullname" . }}-mobile
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "moda.labels" . | nindent 4 }}
    app.kubernetes.io/component: mobile
    app.kubernetes.io/port: {{ .Values.mobile.service.port | quote }}
  {{- with .Values.mobile.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.mobile.service.type }}
  ports:
    - port: {{ .Values.mobile.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moda.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: mobile
{{- end }}
